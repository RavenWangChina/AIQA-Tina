# 全栈全自动开发工作流 · 完整文档

> **© MiaoYu** · 2026年09月01日 · 未经作者许可，本文档不得外传、转载或用于商业用途。

> 版本：2026年09月01日
> 位置：本文档是全局 `~/.claude/CLAUDE.md` 流水线规则的展开手册
> 适用：`<开发主项目>/`（开发主战场，FastAPI + Vite + pytest）；`<文档项目>/` 为方案文档库，仅改文档时不走完整流水线

---

## 一、总体设计

三个关键词：

1. **全自动**——从需求到提交，模型自行推进。权限已配置 `bypassPermissions`（项目级 `.claude/settings.local.json`），不逐项请求授权。
2. **检查点式**——只在三类节点停下来找用户：**需求分歧点**、**不可逆动作**（提交/推送/外发）、**方案级取舍**。其余环节不停。
3. **交叉验证**——关键环节不是单模型单视角跑，而是"主视角 + 对抗视角 + 仲裁规则"的结构化对抗。重复安装的插件（superpowers 两份源、frontend-design 两份源）是刻意保留的，目的是保证挖掘深度和方案质量，不是冗余。

```
需求挖掘 ──→ 方案设计 ──→ 执行 ──→ 审查 ──→ 验证 ──→ 提交
   ↓            ↓          ↓        ↓        ↓        ↓
 双视角对抗   GPT 对抗    TDD     双通道    实证     人工确认
 (分歧→用户)  (逐条回应)  (红→绿)  (都过才走) (没证据=没完成) (说了才提交)
```

每个环节有固定的技能入口（写在全局 CLAUDE.md 里），模型不得随机换技能——这是流程可复现的关键。

---

## 二、六环节详解

### 环节 1：需求挖掘

**目标**：把模糊的想法挖成明确的需求，覆盖工程可行性和业务价值两个面，任何没想清楚的边界都在此环节暴露。

**主视角：`superpowers:brainstorming`**
- 超能力插件的头脑风暴技能，工程视角深挖。
- 强制挖掘三个维度：
  - **边界**——这个功能包含什么、明确不包含什么、输入输出的形状
  - **失败路径**——网络断了怎么办、数据为空怎么办、并发冲突怎么办、上游 API 500 怎么办
  - **数据流**——数据从哪来、经过什么转换、存到哪、谁读它
- 一次只问一个问题（检查点式提问），不甩一张二十个问题的清单给用户。

**交叉验证视角：`product-management:product-brainstorming`**
- 产品视角，换一套完全不同的追问框架：
  - **业务价值**——这个功能解决谁的什么问题，不做的代价是什么
  - **用户故事**——谁、在什么场景下、要完成什么
  - **优先级**——核心路径 vs 锦上添花，MVP 边界划在哪
- 工程视角容易陷入"怎么实现"，产品视角负责拽回"值不值得做、做到什么程度"。

**仲裁规则**
- 两视角结论**一致** → 直接产出需求结论，进入下一环节。
- 两视角结论**不一致** → 这就是"需求分歧点"检查点：把分歧点整理成对照（A 视角认为 X，B 视角认为 Y，各自的理由），停下来交给用户裁决，**不自行取舍**。

**产出物**：需求结论清单（含边界定义、失败路径清单、数据流描述、优先级排序），可直接作为环节 2 的输入。

**入口判定**：任何"新功能/改行为/加组件"的创造性工作，必须先过本环节，不允许跳过直接写代码（superpowers:using-superpowers 的强制规则）。

---

### 环节 2：方案设计

**目标**：把需求结论转成可执行的实施计划——改哪些文件、什么顺序、每步怎么验证。

**主视角：`superpowers:writing-plans`**
- 产出分步实施计划，每步包含：目标文件、具体改动、验证方式。
- 计划里的每一步都要小到可以独立测试——这是环节 3 TDD 能跑起来的前提。
- 计划落在 `docs/superpowers/plans/` 或项目对应目录，是持久文档而非聊天记录。

**交叉验证视角：`claude-code-settings:codex-skill`（GPT 对抗式审查计划）**
- codex CLI 未安装，既定替代方案是**对抗式审查 subagent**（不要再去从 `codex --version` 重查，这是已确认的环境事实）。
- 对抗视角拿到的任务：专门找计划的漏洞——漏改的调用方、没考虑的边界、顺序依赖错误、测试覆盖缺口。

**仲裁规则（逐条回应，不允许"已阅"）**
- GPT/对抗视角提出的**每一个**问题，必须在计划文档或回复中逐条回应：
  - **采纳** → 写明改计划的哪一节、怎么改
  - **不采纳** → 写明为什么不采纳（技术理由，不是"我觉得不用"）
- 这条规则针对的痛点：对抗审查流于形式，提了一堆意见然后石沉大海。

**产出物**：经对抗审查并逐条回应过的实施计划文档。

**检查点**：方案级取舍（两种架构选哪种、要不要引入新依赖等）属于停下来的第三类节点。

---

### 环节 3：执行

**目标**：严格按计划实现，每个子任务有测试兜底。

**主技能：`superpowers:test-driven-development` + `superpowers:subagent-driven-development`**

TDD 纪律（红 → 绿 → 重构）：
1. **先写失败的测试**——测试写完必须跑一遍，确认它真的失败（红）。没红过的测试不算测试先行。
2. **写最小实现**让测试通过（绿）。
3. **重构**在绿灯保护下做。

子代理驱动（SDD）：
- 按计划逐任务派发 subagent 执行，主线程保持清爽，只做调度和验收。
- 每个子任务的产出必须附带测试证据（pytest/vitest 输出）。
- 独立无依赖的任务可用 `superpowers:dispatching-parallel-agents` 并行派发。

**排查 bug 时的入口**：`superpowers:systematic-debugging`——先定位根因再动手，不允许"看着像就改一版试试"。修 bug 的原则是修在所有调用方共同经过的地方，不是只补报错的那条路径。

**产出物**：代码 + 测试 + 每步的测试运行输出。

**不停顿**：执行过程不问权限（bypassPermissions），任务节点只做简报。

---

### 环节 4：审查

**目标**：用两个独立通道审同一份 diff，都过才放行。单通道通过 ≠ 通过。

**主通道：`pr-review-toolkit:review-pr`（本地 diff）**
- 审查未提交的本地改动（git diff 范围）。
- 带完整的专用 agent 班底（代码规范、类型设计、测试覆盖、静默失败猎手等）。
- 审查重点：与项目规范的一致性、风格、潜在问题。

**交叉通道：`coderabbit:coderabbit-review`**
- CodeRabbit AI 独立复审，模型和视角都不同于主通道。
- 两个通道看到的都是同一份 diff，但各自独立出意见。

**通过标准**：**双通道都过**才进下一环节。一个通道提了阻塞级意见，即使另一个通道说没问题，也不放行。

**收到审查意见后的处理：`superpowers:receiving-code-review`**
- 核心纪律：**先验证再改，不盲从**。
- 每条意见先确认"它说的对不对"——重现它描述的问题、核对它引用的代码行。验证成立的才改；不成立的写明理由回绝，不因为"是审查意见"就照单全收。

**产出物**：审查意见处理记录（采纳了什么、回绝了什么及理由）+ 修正后的代码。

---

### 环节 5：验证

**目标**：用可复查的证据证明"完成了"，不接受模型自述。

**主技能：`superpowers:verification-before-completion`**
- 宣布完成之前，必须先运行验证命令、拿到输出、确认结果——证据先于结论，永远。

**实证标准（按改动类型）**：
- **UI 改动 → playwright 截图实证**：用 playwright MCP 打开页面、操作、截图存档（项目里大量 `tmp-*.png` 就是历史实证产物）。
- **后端改动 → pytest 输出实证**：贴出通过的测试运行输出。
- **没有证据 = 没完成**——这是硬规则，无例外。

**测试服访问约定**：验证 test 版统一用测试服域名（以团队内部配置为准，不要用直连 IP 或旧端口入口）。

**产出物**：截图文件 / 测试输出，附在完成报告里。

---

### 环节 6：提交

**主技能：`commit-commands:commit`**

- **触发条件：用户说提交才提交**。这是不可逆动作检查点，全自动到此为止。
- 提交信息按仓库现有风格（看 git log），中文为主，与现有提交历史一致。
- push、开 PR、外发内容同理，均需用户明示。

---

## 三、检查点规则（什么时候会停下来问你）

全自动执行中，只有三类节点停下来：

| 类型 | 例子 | 处理方式 |
|------|------|----------|
| 需求分歧点 | 工程视角和产品视角对边界/优先级结论冲突 | 列分歧对照表（各自观点+理由），用户裁决 |
| 不可逆动作 | git commit / push / 开 PR / 外发内容 / 删除覆盖 | 先问，拿到明确指令再动 |
| 方案级取舍 | 架构二选一、引入新依赖、改公共接口 | 给推荐项和理由，用户拍板 |

其余一切（读文件、写测试、跑测试、改代码、截图验证）自行推进，任务节点做简报即可。

---

## 四、插件与技能全清单

### 4.1 流水线核心技能（六环节固定入口）

| 技能 | 所属插件 | 环节 | 作用 |
|------|----------|------|------|
| `superpowers:brainstorming` | superpowers | 1 | 工程视角需求深挖：边界、失败路径、数据流 |
| `product-management:product-brainstorming` | product-management | 1 | 产品视角交叉验证：业务价值、用户故事、优先级 |
| `superpowers:writing-plans` | superpowers | 2 | 分步实施计划，每步含目标文件/改动/验证方式 |
| `claude-code-settings:codex-skill` | claude-code-settings | 2 | GPT 对抗式审查计划（codex CLI 未装，用对抗 subagent 替代） |
| `superpowers:test-driven-development` | superpowers | 3 | 红→绿→重构，失败先行的测试纪律 |
| `superpowers:subagent-driven-development` | superpowers | 3 | 按计划派发子代理执行，主线程做调度验收 |
| `superpowers:dispatching-parallel-agents` | superpowers | 3 | 独立任务并行派发 |
| `superpowers:systematic-debugging` | superpowers | 3（排查时） | 先根因后修复，拒绝"试试改一版" |
| `pr-review-toolkit:review-pr` | pr-review-toolkit | 4 | 本地 diff 主审查通道，带专用 agent 班底 |
| `coderabbit:coderabbit-review` | coderabbit | 4 | CodeRabbit 独立复审通道 |
| `superpowers:receiving-code-review` | superpowers | 4 | 审查意见处理纪律：先验证再改，不盲从 |
| `superpowers:verification-before-completion` | superpowers | 5 | 证据先于结论：截图/测试输出实证 |
| `commit-commands:commit` | commit-commands | 6 | 提交（用户明示触发） |

### 4.2 superpowers 支撑技能（流程卫生类）

| 技能 | 作用 |
|------|------|
| `superpowers:using-superpowers` | 会话开场规则：相关技能必须先调用再行动，技能优先级：流程技能 > 实现技能 |
| `superpowers:executing-plans` | 在独立会话中按检查点执行既有计划 |
| `superpowers:requesting-code-review` | 完成任务/大功能/合并前主动请求审查 |
| `superpowers:finishing-a-development-branch` | 实现完成后决定分支去留（合并/保留/清理） |
| `superpowers:using-git-worktrees` | 需要隔离的开发工作建立 worktree |
| `superpowers:writing-skills` | 新建/修改技能本身时的规范 |

### 4.3 MCP 服务器（实测状态截至 2026年08月20日）

| MCP | 状态 | 用途 |
|-----|------|------|
| `github` | ✅ 已登录（个人 GitHub 账号） | PR/issue/仓库操作：审查环节看 PR、提 PR、评论 |
| `playwright` | ✅ 正常 | 验证环节主力：页面操作 + 截图实证 |
| `semgrep` | ✅ OAuth 已登录 | 静态代码安全扫描 |
| `mintlify` | ⚠️ 会话内可能断连 | 文档站建设（断连时功能不可用） |
| `context7` | ⚠️ 偶发断连，重启恢复 | 库/框架文档实时查询；断连时用 WebSearch/WebFetch 替代 |
| `chrome-devtools` | ✅ 技能可用，MCP 按需启动 | 浏览器调试：性能、网络、console（playwright 之外的补充通道） |
| `modao`（墨刀） | ✅ 可用但按需 | **仅用户点名时调用**（消耗积分，不进常规流水线） |
| `sentry` / `rootly` | ⚠️ 需各自 token | 事故运维类，当前项目暂未用到 |

### 4.4 设计与前端类

| 技能 | 作用 |
|------|------|
| `frontend-design:frontend-design` | 前端视觉设计方向：排版、美学选择，避免模板脸 |
| `ui-ux-pro-max` | UI/UX 设计智库：79 风格、192 配色、74 字体配对、119 UX 准则，按需检索 |
| `dataviz` | 图表/数据可视化前置必读：图表类型、配色、可访问性 |
| `superdesign` / `design:*`（design-system、design-critique 等） | 设计系统与设计评审 |

### 4.5 辅助与效率类

| 技能 | 作用 |
|------|------|
| `ponytail`（SessionStart hook 常驻） | 懒惰高级开发纪律：先问"要不要存在"再写代码，最短可用 diff，删优于增 |
| `planning-with-files` | Manus 风格文件化规划（task_plan.md / findings.md / progress.md），与 writing-plans 互补 |
| `tianyi-vision` | 走视觉模型 API 识别本地截图（验证环节看图确认），自装于 `~/.claude/skills/`，依赖本地凭据 |
| `Humanizer-zh` | 去除文档的 AI 生成痕迹 |
| `claude-md-management:revise-claude-md` | 用本次会话的收获更新 CLAUDE.md |
| `session-report` | 会话总结报告 |
| `claude-mem:*`（mem-search、do、make-plan 等） | 跨会话记忆与知识库（注入侧不可用，查询侧 mcp-search 可用） |

### 4.6 LSP（2026年08月20日新装）

| 组件 | 版本 | 作用 |
|------|------|------|
| `pyright` | 1.1.413 | Python 类型/定义/引用跳转（配合 pyright-lsp 插件） |
| `typescript-language-server` | 6.0.0 | TypeScript/前端同上（配合 typescript-lsp 插件） |

### 4.7 安全类

| 技能 | 作用 |
|------|------|
| `claude-security:scan` | 全仓多 agent 安全扫描流水线（inventory → 威胁建模 → 研究 → 三视角对抗 → 报告），交给独立的 Security Lead 会话跑 |
| `security-review` / `semgrep` | 快速安全审查 |

---

## 五、支撑设施

### 5.1 权限配置

- 项目级 `.claude/settings.local.json`：`defaultMode: bypassPermissions`——全自动不逐项问权。
- 全局 `settings.json`：预批准常用命令族（git/npm/python/pytest 等）和常用 MCP 工具，减少历史摩擦。
- `additionalDirectories` 放行了开发项目目录等跨目录访问。

### 5.2 记忆体系（两层，一层坏了一层兜底）

- **claude-mem 跨会话注入：不可用**。根因是网关协议限制（所用模型网关是 OpenAI 协议，claude-mem 观察者的 subscription 登录不支持）。用户知情接受，不修。
- **替代纪律：重要决策和状态写进项目文档**（计划文档、进度文档），不依赖记忆注入。
- **本地文件式记忆可用**（`~/.claude/projects/<项目>/memory/`），存环境事实、用户偏好、项目约束，每个会话自动加载索引。

### 5.3 排障约定（环境事实）

- 模型走自建网关（Anthropic 兼容层；网关地址与模型名属本地环境配置，见 `~/.claude/settings.json` 的 env 段）。
- **遇 400 类 API 错误**：先用最小 curl 复现测网关，确认根因在网关侧后，再考虑本地配置——不要先改插件。曾发生的 `server_tool_use` 400 即网关侧问题，网关升级后自愈。
- context7 断连 → WebSearch/WebFetch 替代。
- 测试服验证一律用测试服域名（以团队内部配置为准）。

---

## 六、已知限制与刻意取舍

| 项 | 状态 | 说明 |
|----|------|------|
| 模型单点 | 接受 | 四档模型全指同一网关模型，无 fallback。网关故障 = 全线停摆（曾有 400 事件为样本），暂不处理 |
| claude-mem 注入 | 接受 | 协议限制不可修，用项目文档兜底 |
| 插件重复安装 | **刻意保留** | superpowers（marketplace + dev 双源）、frontend-design（双源）——交叉验证设计的一部分，勿建议删除 |
| 同职能多技能 | **刻意保留** | 审查 4 套、规划 3 套并存；流水线表已固定每环节唯一入口，多套只作后备，不参与随机轮换 |
| sentry/rootly token | 搁置 | 未用到，暂不配 |
| `CLAUDE_CODE_DISABLE_MCP=1` | 失效变量 | 实际 MCP 照常加载，留着无影响但易误导排查，可择机删除 |

---

## 七、一次标准走查（串联示例）

以"给推池申请单加一个批量重新划线功能"为例：

1. **需求挖掘**：superpowers:brainstorming 挖边界（多少条算批量？划线失败的行怎么处理？）和失败路径（部分成功怎么回滚？）；product-management 视角问业务价值（人工划线现在花多久？）。两视角一致 → 产出需求结论。若产品视角认为该功能优先级低于修某个 bug 而工程视角相反 → 停，列分歧给用户。
2. **方案设计**：writing-plans 出分步计划（后端接口 → 前端按钮 → 批量逻辑 → 测试）；对抗 subagent 审出"没考虑并发重复点击"→ 采纳，在计划中补防重逻辑。
3. **执行**：TDD——先写"批量划线接口对空列表返回 400"的测试（跑一次确认红）→ 写实现（绿）；SDD 派子代理做前端按钮部分。
4. **审查**：pr-review-toolkit:review-pr 过本地 diff，coderabbit 独立复审。coderabbit 提"前端没禁用提交中按钮"→ 验证成立 → 改。
5. **验证**：pytest 输出贴出；playwright 打开测试服页面操作批量划线，截图存档。
6. **提交**：**停**。用户说提交，才 commit-commands:commit。
